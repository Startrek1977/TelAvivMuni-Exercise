<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:TelAvivMuni_Exercise.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Shared.xaml provides the neutral brush defaults this dictionary needs.
        Window-hosted elements get live theme brushes from Application.Current.Resources;
        this dictionary (a ResourceDictionary root) cannot reach app resources with
        StaticResource, so it keeps its own copy here.
    -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/TelAvivMuni-Exercise.Themes;component/Themes/Shared.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Browse Button Style (internal part of DataBrowserBox — not a theme concern)  -->
    <Style x:Key="BrowseButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="30" />
        <Setter Property="Background" Value="{StaticResource NeutralLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Padding="3"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1 0 0 0"
                            CornerRadius="0,0,0,0">
                        <Viewbox Width="18" Height="18">
                            <Path Width="24"
                                  Height="24"
                                  Data="M 4,2 H 20 A 2,2 0 0 1 22,4 V 8 A 2,2 0 0 1 20,10 H 4 A 2,2 0 0 1 2,8 V 4 A 2,2 0 0 1 4,2 Z M 4,14 H 20 A 2,2 0 0 1 22,16 V 20 A 2,2 0 0 1 20,22 H 4 A 2,2 0 0 1 2,20 V 16 A 2,2 0 0 1 4,14 Z M 4.5,6 A 1.5,1.5 0 1,0 7.5,6 A 1.5,1.5 0 1,0 4.5,6 M 4.5,18 A 1.5,1.5 0 1,0 7.5,18 A 1.5,1.5 0 1,0 4.5,18"
                                  Fill="Transparent"
                                  Stretch="Uniform"
                                  Stroke="{StaticResource PrimaryDarkBrush}"
                                  StrokeThickness="1" />
                        </Viewbox>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource NeutralMediumBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource NeutralDarkBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  TextBox Style for DataBrowserBox display (internal part — not a theme concern)  -->
    <Style x:Key="DataBrowserTextBoxStyle" TargetType="TextBox">
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="Padding" Value="5 0 25 0" />
        <Style.Triggers>
            <Trigger Property="Opacity" Value="0.5">
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="FontStyle" Value="Italic" />
            </Trigger>
            <Trigger Property="Opacity" Value="1">
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="FontStyle" Value="Normal" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  DataBrowserBox Control Style  -->
    <Style TargetType="{x:Type local:DataBrowserBox}">
        <Setter Property="Background" Value="{StaticResource WhiteBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DataBrowserBox}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <!--  Selected product general icon  -->
                            <Viewbox Grid.Column="0"
                                     Width="18"
                                     Height="18"
                                     Margin="5 0 0 0"
                                     VerticalAlignment="Center">
                                <Path Data="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z M3.29 7 L12 12 L20.71 7 M12 22 L12 12"
                                      Fill="Transparent"
                                      Stretch="Uniform"
                                      Stroke="{StaticResource PrimaryDarkBrush}"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeThickness="2" />
                            </Viewbox>
                            <!--  Wrap TextBox in inner Grid for overlay  -->
                            <Grid Grid.Column="1">
                                <TextBox x:Name="PART_TextBox"
                                         Opacity="0.5"
                                         Style="{StaticResource DataBrowserTextBoxStyle}"
                                         Text="Click to select..." />

                                <Button x:Name="PART_ClearButton"
                                        Width="20"
                                        Height="20"
                                        Margin="5 0 5 0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Content="&#x2715;"
                                        Cursor="Hand"
                                        FontSize="12"
                                        Foreground="{StaticResource TextDisabledBrush}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource NeutralHoverBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                            <Button x:Name="PART_BrowseButton"
                                    Grid.Column="2"
                                    Style="{StaticResource BrowseButtonStyle}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--
                            Hide the clear button when nothing is selected.
                            A ControlTemplate Trigger reads the DP directly via the property engine,
                            which guarantees change-notification tracking — unlike a DataTrigger binding
                            inside an inline Button.Style, which can silently miss updates to a
                            read-only DP after the initial template application.
                        -->
                        <Trigger Property="HasSelection" Value="False">
                            <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
